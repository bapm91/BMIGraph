<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/fragment_grad">


    <TextView
        android:id="@+id/result_bmi"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:maxLines="5"
        android:minLines="4"
        android:textColor="@color/white_dark"
        android:textSize="@dimen/result"
        app:layout_constraintBottom_toTopOf="@+id/rotate_bmi"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/enter_height"/>

    <Button
        android:id="@+id/rotate_bmi"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/button_grad"
        android:text="@string/rotate"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/result_bmi"/>

    <EditText
        android:id="@+id/enter_weight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/perimeter_view_line"
        android:ems="10"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:hint="@string/weight"
        android:inputType="numberDecimal"
        android:maxEms="6"
        android:padding="@dimen/edit_padding"
        android:textColor="@color/white_dark"
        android:textColorHint="@color/white_dark"
        app:layout_constraintBottom_toTopOf="@+id/enter_height"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <EditText
        android:id="@+id/enter_height"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/perimeter_view_line"
        android:ems="10"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:hint="@string/height"
        android:inputType="numberDecimal"
        android:maxEms="6"
        android:padding="@dimen/edit_padding"
        android:textColor="@color/white_dark"
        android:textColorHint="@color/white_dark"
        app:layout_constraintBottom_toTopOf="@+id/result_bmi"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/enter_weight"/>

    <TextView
        android:id="@+id/text_view_weight_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:layout_marginTop="8dp"
        android:text="@string/weight_hint"
        android:textColor="@color/white_dark"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="@+id/enter_weight"
        app:layout_constraintRight_toLeftOf="@+id/enter_weight"
        app:layout_constraintTop_toTopOf="@+id/enter_weight"
        android:layout_marginRight="8dp"/>

    <TextView
        android:id="@+id/text_view_height_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:layout_marginTop="8dp"
        android:text="@string/height_hint"
        android:textColor="@color/white_dark"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="@+id/enter_height"
        app:layout_constraintRight_toLeftOf="@+id/enter_height"
        app:layout_constraintTop_toTopOf="@+id/enter_height"
        android:layout_marginRight="8dp"/>

</android.support.constraint.ConstraintLayout>